CREATE TABLE LÅNETILBUD
(
lånetilbud_id INTEGER IDENTITY PRIMARY KEY,
rentesats DOUBLE,
tilbagebetalingsperiode INTEGER,
udbetaling INTEGER,
ÅOP DOUBLE,
kunde_id INTEGER FOREIGN KEY REFERENCES kunde(kunde_id),
bil_id INTEGER FOREIGN KEY REFERENCES bil(bil_id),
sælger_id INTEGER FOREIGN KEY REFERENCES sælger(sælger_id),
oprettelsestidspunkt TIMESTAMP,
)

CREATE TABLE SÆLGER
(
sælger_id INTEGER IDENTITY PRIMARY KEY,
navn VARCHAR(250),
rang VARCHAR(50),
)

CREATE TABLE KUNDE
(
kunde_id INTEGER IDENTITY PRIMARY KEY,
CPRnummer VARCHAR(10) UNIQUE NOT NULL,
navn VARCHAR(250),
adresse VARCHAR(80),
postnummer VARCHAR(4),
bynavn VARCHAR(50),
telefon VARCHAR(11),
kommentar VARCHAR(250)
)

CREATE TABLE BIL
(
bil_id INTEGER IDENTITY PRIMARY KEY,
pris DOUBLE
)

SELECT * FROM lånetilbud INNER JOIN lånetilbud.bil_id ON bil.bil_id;


INSERT INTO "PUBLIC"."SÆLGER"
(  "NAVN", "RANG" )
VALUES ( 'Anders Looft', 'CEO')


INSERT INTO "PUBLIC"."KUNDE"
(  "CPRNUMMER", "NAVN", "ADRESSE", "POSTNUMMER", "BYNAVN", "TELEFON", "KOMMENTAR" )
VALUES ( '0807911449', 'Anders Looft', 'Ny Møllevej 14A 1 tv', '7400', 'Herning', '60150191', '')


INSERT INTO "PUBLIC"."BIL" ( "PRIS" ) VALUES ( 1000000 );
INSERT INTO "PUBLIC"."BIL" ( "PRIS" ) VALUES ( 2500000 );
INSERT INTO "PUBLIC"."BIL" ( "PRIS" ) VALUES ( 3100000 );

INSERT INTO "PUBLIC"."LÅNETILBUD"
(  "RENTESATS", "TILBAGEBETALINGSPERIODE", "UDBETALING", "ÅOP", "KUNDE_ID", "BIL_ID", "SÆLGER_ID", "OPRETTELSESTIDSPUNKT" )
VALUES ( '7.3', '18' , '500000' , '1.3', '0', '0', '0', '2015-06-05 09:24:08')




SELECT rentesats, tilbagebetalingsperiode, udbetaling, ÅOP, oprettelsestidspunkt, pris, kunde.CPRnummer, kunde.navn, kunde.adresse, kunde.postnummer, kunde.bynavn, kunde.telefon, kunde.kommentar, sælger.navn, sælger.rang  FROM lånetilbud INNER JOIN bil ON lånetilbud.bil_id = bil.bil_id INNER JOIN kunde ON lånetilbud.kunde_id = kunde.kunde_id INNER JOIN sælger ON lånetilbud.sælger_id = sælger.sælger_id;
SELECT rentesats, tilbagebetalingsperiode, udbetaling, ÅOP, oprettelsestidspunkt, pris, kunde.CPRnummer, kunde.navn AS kundenavn, kunde.adresse, kunde.postnummer, kunde.bynavn, kunde.telefon, kunde.kommentar, sælger.navn AS sælgernavn, sælger.rang  FROM lånetilbud INNER JOIN bil ON lånetilbud.bil_id = bil.bil_id INNER JOIN kunde ON lånetilbud.kunde_id = kunde.kunde_id INNER JOIN sælger ON lånetilbud.sælger_id = sælger.sælger_id;